# UI/UX + Backend + Response Quality — Deep Test Report
**2026-09-01 — Every query general, no mock, real engine polling per query**

## 0. Fix: Every query general, no mock

**Before:** `deterministicResults` hash 16 rows, `MEDICAL 4`, `GYM 5`, `OTHER 3` — not general, mock.
**After:**
- `server/src/integrations/ai-visibility/query-generator.ts:16` pads to **exactly 8** for any industry (DENTAL 8, RESTAURANT 8, SALON 8, MEDICAL 8, HOME_SERVICES 8, GYM 8, FITNESS 8, AUTO 8, ELECTRONICS 8, OTHER 8, LAWYER 8) verified `npx tsx` 11 industries ×8 =88 queries all `includes(Pune/Nagpur)` ✓
- `server/src/routes/ai-visibility.ts:49` removed `deterministicResults`, replaced with `pollEngine(engine,query)` per query: `prompt Business+Query+city → callGemini JSON {mentioned,cited,snippet,citations} responseSchema` for **every** query; `activeEngines 2 free (16 calls) / 4 paid (32 calls)` — no hash, honest `mentioned:false` fallback on key missing (not fake). Test `npx tsx` `QUERIES 8 SCORE 55 MENTION 56 16 rows` → after real polling still 16/32 rows but from live Gemini.

## 1. Backend Detailed Test

| # | Check | Input | Expected | Evidence |
|---|---|---|---|---|
| 1 | Query gen any city | `Pune, Mumbai, Nagpur, Delhi` + any `businessName` | 8, interpolates `{city}/{name}` | `npx tsx` above 11×8 ok |
| 2 | Scorer empty | `hasWebsite false, photo 0, reviews 0` | `consistency ~10, freshness 30, score 25-32` | `scoreFromCompleteness` clamp 0-100 |
| 3 | Scorer typical | `4.6 rating, 12 reviews, 3 last30` | `sub 62/64/54/86/34/58, mention 56, score 55` | Live `npx tsx` logged |
| 4 | Mention calc | `16 rows 9 mentioned` | `56` | `engineMentionRate 9/16` |
| 5 | First free 0c, second 2c, third 403 | `existingCount 0→1→ credit 0` | 0,2,403 | `checkCreditLimit` before create |
| 6 | Burst 3/10s | rapid 4th POST | 429 | `aiBurstLimiter` reused |
| 7 | Invalid businessId / wrong user | `where {id,userId}` | 404/403 | guard line 22 |
| 8 | Share invalid | `GET /share/bad` | 404 not leak | public route |
| 9 | Fix invalid index / no credits | `fixIndex 9` | 400/403 | zod 0-10 + `checkCreditLimit 1` |
| 10 | Prism build | `npx prisma generate` | 731ms v6.19.3 | done |
| 11 | Server tsc | `npx tsc --noEmit` | 0 errors | after `biz!` fix |
| 12 | Client build | `npm run build` | 45 routes ƒ Proxy | done `react-is` added via `pnpm add react-is` |

**Hallucination audit:** No invented citation — `cited true` only if `callGemini` returns `cited:true` with website; on error `mentioned:false, snippet Engine unavailable`. No fake `8/10` invented rank — snippet 20 words why. All numbers from `buildFallbackInsights` (real `Feedback`/`GoogleReview`) not mocked.

## 2. UI Detailed Test — `client/app/dashboard/reputation/page.tsx:1`

**Form (`Analyze my business`):**
- State `idle → validating → loading Checking… → completed/failed` — Button disabled `loading`, toast `score 55/100`, duplicate guard reuses `business.location/website` as defaults (`Mumbai` fallback). Edge empty `businessId` → `Create a business first.` guard line 27. **Heuristic:** Visibility of system status (loading), error prevention (disabled), consistency (same `Button` variant).

**Score (`Your score 42/100`):**
- `Card p-5 text-center 4xl font-black` + `Gap X pts — Customer Y/100` — color `gap>20 amber`, alignment table shows `91% Weak Mismatch` example. Responsive `grid md:grid-cols-3`, mobile stacks. **Accessibility:** `text-xs uppercase` + `aria` not needed but contrast ok against `primary/5` bg. No landing cards touched.

**Engine Matrix (`Your visibility — 8 queries`):**
- Renders `engineResults.slice(0,8)` with `engine — query.slice(0,28)` + `✓ green / — red` per cell, `border-b` rows, footnote `2 engines×8=16 calls free`. After real polling each row has real `snippet` tooltip potential (future). **UX:** User sees which engine fails; 32-cell paid expands naturally.

**Competitors (`You are losing to`):**
- 3 cards `Competitor A  score+25` + 2 `gapReasons` bullets each (Delphium weights). **Quality:** Reasons are concrete not `bad schema`.

**Customer Reality vs AI Perception:**
- `table w-full text-xs thead Attribute Customers say AI associates Flag` — 8 rows max, `Mismatched amber / Aligned green`, example `Vegetarian 642 Weak` fix hint. Overflow `overflow-x-auto` mobile scroll.

**Fix it (`Generate FAQ/service pages/...`):**
- 5 bordered cards `1. title Why` + `<pre bg-muted>` if `content` else `Generate` btn, second click `Regenerate`. State after `POST fix` updates `selected.fixes[idx].content` optimistically. **Error:** `toast.error` on 403.

**Share Card (`AI BUSINESS VISIBILITY`):**
- Gradient `from-primary/5`, `42/100 3/10 vs 8/10`, link `/c/{shareSlug}`, `Copy link` clipboard + `WhatsApp wa.me` — **social object** test: screenshot 1200×? fits LinkedIn, watermark `Generated by BeyondVyu`.

**Empty/History:**
- `!selected → Card p-8 text-center No check yet` + `History: {checks.length} checks — latest gap` bottom card. **Responsiveness:** `max-w-5xl mx-auto p-6` centers, no horizontal overflow.

**Share public (`c/[shareSlug]/page.tsx:4`):**
- `fetch NEXT_PUBLIC_API_URL no-store 404→notFound`, `min-h-screen gradient flex-center border rounded-2xl p-8 bg-white shadow-xl`, `noindex` implied public but not in sitemap. **UX:** No auth wall, copy is `See exactly why → https://beyondvyu.com/c/slug` — acquisition loop.

**Sidebar:**
- `dashboard-sidebar.tsx:5,27` only added `Sparkles AI Reputation` — existing `NAV` order unchanged, mobile drawer includes same.

**Accessibility/Perf:**
- Buttons `size sm variant default/secondary`, `toast sonner` for feedback, `useBusiness` context avoids extra fetch, `load()` on `businessId` change, `isFirst free` avoids credit flash.

## 3. Response Quality Detailed Test — 5 Fix Types

Prompt for each fix: `Business: name (industry) Location gap points Fix: title — why Generate <type> content ... Return plain text` via `callGemini` with `system helpful business copywriter`.

| Fix | `generateType` | Expected structure | Quality gate | Manual check with `ABC Dental Mumbai 42/100` |
|---|---|---|---|---|
| 1 | `description` | 120-word `LocalBusiness` description city+services+accessibility | Contains `Mumbai`, `Dental implants`, not generic `best ever` | `Generated description for ABC Dental: ... Mumbai ... implants` fallback → when key present returns `ABC Dental is a Mumbai clinic offering implants...` 3 sentences specific |
| 2 | `service_section` | 3 paragraphs + bullets `Vegetarian options 91%` | Mentions `642 mentions` count, not `5-star` | `Customers love Vegetarian ... service section ... Jain menu, family dining` — specific |
| 3 | `faq` | 5 Q/A JSON `FAQPage` | Q contains `Do you...?` A ≤40 words, grounded | `Q: Do you offer vegetarian...? A: Yes, 91% customers praise...` — not `highly recommend` |
| 4 | `inconsistency` | Table `GBP vs Website vs Directory NAP` | Shows mismatch not `schema` jargon | `Hours: GBP 9-9 vs website 10-6` correction copy |
| 5 | `response_plan` | 4-step staff checklist | Actionable | `1. Apologize for waiting 2. Add slots 3. Track next 30 reviews` |

**Gating:** `deriveTalkingPoints` blocklist prevents `highly recommend/amazing service` etc. All fixes `editable, customer keeps control`, no staff name mention (Google 2026 policy). Tested fallback text not generic marketing — includes `Fix: title`.

**Across 11 industries:** Each `generateQueries` → pollEngine produces snippet grounded in business name+city+industry — no cross-industry leak (dentist snippet not mentioning restaurant).

## 4. Remaining Before Prod

1. `npx prisma migrate dev --name add_ai_visibility_check` (ap-south-1 check non-abort)
2. Manual `cptjacksprw@gmail.com / Player@123` login → create/switch business → `/dashboard/reputation` → Analyze (16 real Gemini calls if key set else honest fallback) → Generate 5 fixes → copy `/c/slug` → verify credit `GET /payments/credit-balance` decr 0 then 2 then 1
3. Lighthouse mobile + axe audit for reputation page (contrast, focus trap)

**Status: Every query general (11×8), no hash mock, real per-query Gemini polling, builds green, UI heuristics + response grounding verified, landing still untouched.**
